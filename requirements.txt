# ── Integrated simulator dependencies ──────────────────────────
# Shared by logistics (main_ui) + production (LogiFabSim)

# GUI (logistics simulator + integrated UI)
PyQt6

# Production simulator (LogiFabSim / PySCFabSim)
numpy
pandas
sortedcontainers
python-dateutil
pytz
six
tzdata

# Batch experiments / parallel execution (optional) — keeps the original LogiFabSim main.py features
joblib
tqdm

# Note:
# - cffi / greenlet / hpy are only needed when using the PyPy interpreter;
#   they do not need to be installed in a regular CPython environment.

# C-accelerated path search (fast_pathfinder)
scipy
