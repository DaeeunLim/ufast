# Runtime dependencies, mirroring the [project] dependencies in pyproject.toml.
#
# The recommended install is  pip install -e .  which reads pyproject.toml and
# also registers the ufast-run / ufast-fromto / ufast-analyze / ufast-aggregate
# commands. This file is kept for environments that prefer a requirements file.
#
# Optional extras (not listed here):
#   pip install -e ".[viz]"   Rerun replay, charts, HTML report
#   pip install -e ".[gui]"   PyQt6 desktop GUI

numpy
pandas
scipy            # C Dijkstra acceleration for fast_pathfinder
python-dateutil
pytz
six
tzdata
joblib
tqdm
