# ── 통합 시뮬레이터 의존성 ──────────────────────────────────
# 물류(main_ui) + 생산(LogiFabSim) 공통

# GUI (물류 시뮬레이터 + 통합 UI)
PyQt6

# 생산 시뮬레이터 (LogiFabSim / PySCFabSim)
numpy
pandas
sortedcontainers
python-dateutil
pytz
six
tzdata

# 배치 실험/병렬 실행(선택) — 원본 LogiFabSim main.py 기능 유지용
joblib
tqdm

# 참고:
# - cffi / greenlet / hpy 는 PyPy 인터프리터 사용 시에만 필요하며
#   일반 CPython 환경에서는 설치하지 않아도 됩니다.

# 경로탐색 C 가속 (fast_pathfinder)
scipy
